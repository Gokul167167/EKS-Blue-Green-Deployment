apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
      color: green
  template:
    metadata:
      labels:
        app: frontend
        color: green
    spec:
      containers:
        - name: frontend
          image: 748827982277.dkr.ecr.us-east-1.amazonaws.com/ecommerce:frontend-latest2
          ports:
            - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-green-service
spec:
  selector:
    app: frontend
    color: green
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP